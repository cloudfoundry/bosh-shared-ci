---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: concourse/oci-build-task

outputs:
  - name: image

params:
  CONTEXT: .
  DOCKERFILE: Dockerfile

run:
  path: build
